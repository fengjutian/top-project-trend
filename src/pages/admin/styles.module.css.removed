:global(body.content-studio-page .navbar) { display: none; }
:global(body.content-studio-page) { overflow: hidden; }
.studio { height: 100vh; display: grid; grid-template-columns: 300px 1fr; overflow: hidden; background: #f6f7fb; color: #202234; }
.studioFocus { grid-template-columns: 1fr; }
.studioFocus .sidebar { display: none; }
.sidebar { background: #111425; color: #f7f8ff; padding: 24px 18px; display: flex; flex-direction: column; height: 100vh; }
.brand { display: flex; align-items: center; gap: 12px; margin-bottom: 24px; }
.brand > span { display: grid; place-items: center; width: 38px; height: 38px; border-radius: 12px; font-weight: 800; background: linear-gradient(135deg, #6d5dfc, #38bdf8); }
.brand div { display: flex; flex-direction: column; }
.brand small { color: #858ba6; font-size: 11px; }
.deployStatus { display: flex; align-items: center; gap: 7px; margin: -12px 0 16px; color: #aeb4cf; font-size: 11px; text-decoration: none; }
.deployStatus:hover { color: white; text-decoration: none; }
.deployStatus > span { width: 7px; height: 7px; border-radius: 50%; }
.deploySuccess > span { background: #34d399; }
.deployRunning > span { background: #fbbf24; box-shadow: 0 0 0 4px rgba(251,191,36,.12); }
.deployFailed > span { background: #fb7185; }
.search { display: flex; align-items: center; gap: 7px; background: #1b1f34; border: 1px solid #292e49; border-radius: 10px; padding: 0 10px; margin: 0 0 10px; }
.search input, .sidebar select { width: 100%; color: #eef0ff; background: transparent; border: 0; outline: 0; padding: 10px 4px; }
.sidebar select { background: #1b1f34; border: 1px solid #292e49; border-radius: 10px; margin-bottom: 10px; }
.filters { display: grid; grid-template-columns: repeat(3, 1fr); gap: 5px; margin-bottom: 10px; padding: 4px; border-radius: 10px; background: #1b1f34; }
.filters button { border: 0; border-radius: 7px; padding: 7px 4px; color: #838aa7; background: transparent; font-size: 12px; cursor: pointer; }
.filters button.filterActive { color: white; background: #343a5a; }
.newButton { border: 0; border-radius: 10px; padding: 11px; background: #6d5dfc; color: white; font-weight: 700; cursor: pointer; }
.dashboardButton { margin-bottom: 8px; border: 1px solid #3a4060; border-radius: 9px; padding: 9px; color: #d9dcf0; background: transparent; cursor: pointer; }
.batchBar { margin: 10px 0 0; padding: 9px; border-radius: 9px; background: #282e4c; font-size: 12px; }
.batchBar > div { display: flex; gap: 4px; margin-top: 7px; }
.batchBar button { flex: 1; border: 0; border-radius: 5px; padding: 5px; color: #dfe2f6; background: #3a4165; cursor: pointer; }
.articleList { overflow: auto; flex: 1; margin: 18px -6px 0; padding: 0 6px; }
.articleRow { display: flex; align-items: center; border-radius: 9px; padding-left: 8px; }
.articleRow > input { flex: 0 0 auto; accent-color: #786bf2; }
.articleRow:hover, .articleRow.active { background: #222741; }
.articleItem { min-width: 0; flex: 1; text-align: left; color: #cdd1e7; background: transparent; border: 0; border-radius: 9px; padding: 10px 8px; cursor: pointer; display: flex; flex-direction: column; gap: 3px; }
.articleItem strong { overflow: hidden; text-overflow: ellipsis; white-space: nowrap; }
.articleItem span, .muted { color: #777e9d; font-size: 12px; }
.articleItem:hover, .articleRow.active .articleItem { color: white; }
.logout { margin-top: 12px; border: 1px solid #303550; border-radius: 9px; padding: 9px; color: #9299b8; background: transparent; cursor: pointer; }
.editor { min-width: 0; height: 100vh; overflow: auto; }
.toolbar { height: 72px; padding: 0 34px; display: flex; align-items: center; justify-content: space-between; background: white; border-bottom: 1px solid #e7e8ef; position: sticky; top: 0; z-index: 2; }
.toolbar > div:first-child { display: flex; align-items: center; gap: 12px; min-width: 0; }
.toolbar small { color: #85889a; overflow: hidden; text-overflow: ellipsis; white-space: nowrap; }
.draft, .published { border-radius: 999px; padding: 5px 9px; font-size: 12px; font-weight: 700; }
.draft { color: #9a6500; background: #fff1c2; }
.published { color: #08755c; background: #d7f7ec; }
.unsaved { color: #b14d00; font-size: 12px; white-space: nowrap; }
.toolbarActions { display: flex; gap: 9px; }
.toolbar button, .inlineInput button, .uploadButton { border: 1px solid #d7d9e5; background: white; border-radius: 8px; padding: 8px 13px; cursor: pointer; white-space: nowrap; font-size: 14px; font-weight: 400; color: #202234; line-height: 1.25; }
.toolbar button.primary { color: white; border-color: #5c4ee5; background: #5c4ee5; }
.toolbar button.danger { color: #b42318; }
.toolbar button:disabled, .uploadButton:has(input:disabled) { opacity: .45; cursor: not-allowed; }
.uploadButton input { display: none; }
.issueCount { display: inline-grid; place-items: center; min-width: 18px; height: 18px; margin-left: 3px; border-radius: 99px; color: #8c4d00; background: #ffe3a8; font-size: 10px; }
.auditWrapper { position: relative; display: inline-flex; }
.auditPopover { position: absolute; top: calc(100% + 8px); right: 0; min-width: 280px; max-width: 360px; padding: 8px; border-radius: 10px; background: white; box-shadow: 0 12px 36px rgba(27, 30, 54, .18); border: 1px solid #e5e7ef; z-index: 5; display: grid; gap: 6px; }
.auditItem { display: grid; grid-template-columns: auto 1fr; gap: 8px; align-items: start; padding: 8px 10px; border-radius: 7px; font-size: 12px; line-height: 1.4; }
.auditItem strong { font-size: 10px; padding: 2px 6px; border-radius: 99px; }
.auditItemError { background: #fef3f2; color: #b42318; }
.auditItemError strong { background: #fde0dd; color: #b42318; }
.auditItemWarn { background: #fff8eb; color: #8c4d00; }
.auditItemWarn strong { background: #ffe3a8; color: #8c4d00; }
.auditMore { padding: 8px 10px; border-radius: 7px; background: #f6f7fb; color: #5c6075; font-size: 11px; cursor: pointer; }
.auditEmpty { padding: 10px; color: #85899b; font-size: 12px; text-align: center; }
.moreWrapper { position: relative; display: inline-flex; }
.moreWrapper > button { padding: 8px 12px; font-size: 16px; line-height: 1; }
.moreMenu { position: absolute; top: calc(100% + 8px); right: 0; min-width: 140px; padding: 6px; border-radius: 10px; background: white; box-shadow: 0 12px 36px rgba(27, 30, 54, .18); border: 1px solid #e5e7ef; z-index: 5; display: grid; gap: 2px; }
.moreMenu button { width: 100%; text-align: left; border: 0; background: transparent; border-radius: 7px; padding: 8px 10px; cursor: pointer; color: #202234; font-size: 14px; line-height: 1.25; font-weight: 400; }
.moreMenu button:hover:not(:disabled) { background: #f6f7fb; }
.moreMenu button:disabled { color: #a0a4b4; cursor: not-allowed; }
.message { margin: 18px auto 0; max-width: 980px; border-radius: 10px; padding: 11px 14px; color: #554a97; background: #ece9ff; display: flex; align-items: center; gap: 12px; }
.message > span { flex: 1; min-width: 0; word-break: break-word; }
.messageError { color: #b42318; background: #fef3f2; }
.messageClose { border: 0; background: transparent; padding: 0 6px; cursor: pointer; color: inherit; opacity: .55; font-size: 18px; line-height: 1; }
.messageClose:hover { opacity: 1; }
.form, .preview { max-width: 980px; margin: 0 auto; padding: 36px 42px 80px; }
.form > label, .grid label { display: flex; flex-direction: column; gap: 7px; margin-bottom: 18px; color: #5c6075; font-size: 13px; font-weight: 650; }
.form input, .form textarea { width: 100%; border: 1px solid #dfe1eb; border-radius: 9px; padding: 10px 12px; background: white; color: #25283b; outline: 0; }
.form input:focus, .form textarea:focus { border-color: #7567ed; box-shadow: 0 0 0 3px rgba(109,93,252,.1); }
.form .titleInput { border: 0; border-bottom: 1px solid #e4e5ed; border-radius: 0; padding: 8px 0 18px; margin-bottom: 26px; background: transparent; font-size: 34px; font-weight: 800; }
.templateBar { display: flex; align-items: center; gap: 7px; margin-bottom: 22px; padding: 10px 12px; border-radius: 10px; background: #eeecff; }
.templateBar span { margin-right: auto; color: #645ba0; font-size: 12px; font-weight: 700; }
.templateBar button { border: 1px solid #d4d0fa; border-radius: 7px; padding: 6px 9px; color: #5548ba; background: white; cursor: pointer; }
.grid { display: grid; grid-template-columns: 1fr 1.6fr 1fr .7fr; gap: 14px; }
.grid .checkbox { flex-direction: row; align-items: center; align-self: center; margin-top: 14px; }
.checkbox input { width: auto; }
.scheduleGrid { display: grid; grid-template-columns: 1fr 1fr; gap: 14px; margin-bottom: 4px; }
.scheduleGrid label { display: flex; flex-direction: column; gap: 7px; color: #5c6075; font-size: 13px; font-weight: 650; }
.scheduleGrid small { color: #9296a8; font-weight: 400; }
.inlineInput { display: flex; align-items: flex-start; gap: 8px; }
.bodyLabel textarea { min-height: 420px; resize: vertical; font: 14px/1.7 ui-monospace, SFMono-Regular, Consolas, monospace; }
.documentMeta { display: flex; flex-wrap: wrap; gap: 14px; margin: 3px 0 10px; color: #85899b; font-size: 11px; }
.writingArea { display: grid; grid-template-columns: minmax(0, 1fr) 190px; gap: 18px; align-items: start; }
.writingArea .bodyLabel { margin: 0; }
.outline { position: sticky; top: 88px; display: grid; gap: 7px; max-height: 420px; overflow: auto; padding: 13px; border: 1px solid #e0e2eb; border-radius: 10px; background: white; }
.outline strong { margin-bottom: 4px; font-size: 12px; }
.outline span { overflow: hidden; color: #676b80; font-size: 11px; text-overflow: ellipsis; white-space: nowrap; }
.outline small { color: #a0a4b4; line-height: 1.5; }
.preview { background: white; margin-top: 32px; margin-bottom: 60px; border: 1px solid #e5e7ef; border-radius: 18px; box-shadow: 0 14px 50px rgba(27,30,54,.06); }
.preview > h1 { font-size: 40px; margin-bottom: 12px; }
.previewMeta { color: #7a7e93; font-size: 13px; }
.lead { color: #676b7d; font-size: 18px; }
.previewBody { line-height: 1.8; }
.previewBody img { display: block; max-width: 100%; margin: 20px auto; border-radius: 10px; }
.previewBody pre { overflow: auto; padding: 16px; border-radius: 10px; background: #171a29; color: #eef0ff; }
.previewError { padding: 20px; border: 1px solid #fecaca; border-radius: 10px; color: #991b1b; background: #fff1f2; }
.previewError p { margin: 8px 0; font-family: ui-monospace, SFMono-Regular, Consolas, monospace; }
.previewError small { color: #b45353; }
.previewLoading { padding: 50px; text-align: center; color: #85899b; }
.connectCard { max-width: 540px; margin: 90px auto; padding: 42px; border: 1px solid #e2e4ee; border-radius: 20px; background: white; box-shadow: 0 20px 70px rgba(31,36,69,.1); }
.connectCard h1 { margin: 8px 0 10px; }
.connectCard p { color: #6b6f83; }
.connectCard input { width: 100%; padding: 12px; border: 1px solid #d9dce8; border-radius: 9px; margin: 12px 0; }
.connectCard button { width: 100%; padding: 12px; border: 0; border-radius: 9px; color: white; background: #5c4ee5; font-weight: 700; cursor: pointer; }
.eyebrow { color: #6556e9; font-weight: 800; font-size: 12px; letter-spacing: .16em; }
.loading { padding: 80px; text-align: center; }
.modalBackdrop { position: fixed; inset: 0; z-index: 1000; display: grid; place-items: center; padding: 30px; background: rgba(10, 13, 27, .68); backdrop-filter: blur(5px); }
.modal { width: min(980px, 94vw); max-height: 86vh; overflow: hidden; display: flex; flex-direction: column; border-radius: 18px; background: #f8f9fc; box-shadow: 0 28px 90px rgba(0, 0, 0, .28); }
.modal > header { display: flex; align-items: center; justify-content: space-between; padding: 20px 24px; border-bottom: 1px solid #e1e3eb; background: white; }
.modal h2 { margin: 3px 0 0; }
.modal > header > button { border: 0; background: transparent; color: #73778b; font-size: 28px; cursor: pointer; }
.historyList, .mediaGrid { overflow: auto; padding: 20px; }
.historyItem { display: flex; align-items: center; justify-content: space-between; gap: 20px; padding: 14px 16px; border-bottom: 1px solid #e3e5ec; }
.historyItem > div { min-width: 0; display: grid; gap: 3px; }
.historyItem strong { overflow: hidden; text-overflow: ellipsis; white-space: nowrap; }
.historyItem span { color: #777b8f; font-size: 12px; }
.historyItem code { width: fit-content; color: #6256be; font-size: 11px; }
.historyItem button, .mediaItem button { border: 1px solid #d8dae5; border-radius: 7px; padding: 7px 10px; background: white; cursor: pointer; white-space: nowrap; }
.mediaGrid { display: grid; grid-template-columns: repeat(auto-fill, minmax(180px, 1fr)); gap: 14px; }
.mediaItem { min-width: 0; padding: 10px; display: grid; gap: 7px; border: 1px solid #e0e2ea; border-radius: 11px; background: white; }
.mediaItem img { width: 100%; height: 125px; object-fit: contain; border-radius: 7px; background: #eef0f5; }
.mediaItem strong { overflow: hidden; text-overflow: ellipsis; white-space: nowrap; font-size: 13px; }
.mediaItem > span { color: #85899b; font-size: 11px; }
.mediaItem > div { display: flex; gap: 6px; }
.mediaItem button { flex: 1; }
.mediaItem button.danger { color: #b42318; }
.panelEmpty { grid-column: 1 / -1; padding: 60px 20px; text-align: center; color: #85899b; }
.dashboard { min-width: 0; height: 100vh; padding: 38px; overflow: auto; }
.dashboard > header { display: flex; justify-content: space-between; align-items: center; max-width: 1100px; margin: 0 auto 24px; }
.dashboard h1 { margin: 3px 0 0; }
.dashboard > header button { border: 1px solid #d8dae5; border-radius: 9px; padding: 9px 14px; background: white; cursor: pointer; }
.dashboardActions { display: flex; gap: 8px; }
.metricGrid { max-width: 1100px; margin: 0 auto 24px; display: grid; grid-template-columns: repeat(3, 1fr); gap: 13px; }
.metricGrid article { padding: 20px; border: 1px solid #e1e3eb; border-radius: 13px; background: white; }
.metricGrid span { display: block; color: #74788d; font-size: 13px; }
.metricGrid strong { display: block; margin-top: 8px; font-size: 30px; }
.healthTable { max-width: 1100px; margin: 0 auto; padding: 22px; border: 1px solid #e1e3eb; border-radius: 14px; background: white; }
.healthTable h2 { margin-top: 0; }
.healthTable > div { width: 100%; display: flex; align-items: center; justify-content: space-between; padding: 12px 5px; border-top: 1px solid #eceef3; }
.healthTable > div > span { display: grid; gap: 3px; }
.healthTable small { color: #868a9d; }
.healthTable em { border-radius: 99px; padding: 4px 8px; font-size: 11px; font-style: normal; }
.issueError { color: #a62b21; background: #fee4e2; }
.issueWarn { color: #8c5700; background: #fff0c7; }
.issueOk { color: #08755c; background: #d7f7ec; }
.auditList, .tagList { overflow: auto; padding: 20px; }
.auditList > div { display: flex; align-items: flex-start; gap: 12px; padding: 13px; margin-bottom: 8px; border-radius: 9px; }
.auditList > div > span { border-radius: 99px; padding: 3px 7px; font-size: 11px; }
.auditList p { margin: 0; }
.auditError { background: #fff0ef; }
.auditError > span { color: #a62b21; background: #ffd6d2; }
.auditWarn { background: #fff8e7; }
.auditWarn > span { color: #8c5700; background: #ffe8aa; }
.auditSuccess { color: #08755c; background: #dcf8ee; }
.auditList .auditNote { margin-top: 18px; color: #868a9d; font-size: 12px; }
.tagList { display: grid; gap: 8px; }
.tagList button { display: grid; grid-template-columns: 1fr auto auto; gap: 16px; align-items: center; padding: 12px 14px; border: 1px solid #e1e3eb; border-radius: 9px; background: white; text-align: left; cursor: pointer; }
.tagList span { color: #72768a; }
.tagList em { color: #6556e9; font-size: 12px; font-style: normal; }
.calendarPanel, .reportPanel { max-width: 1100px; margin: 0 auto 24px; padding: 22px; border: 1px solid #e1e3eb; border-radius: 14px; background: white; }
.calendarPanel > header, .reportPanel > header { display: flex; align-items: center; justify-content: space-between; gap: 15px; margin-bottom: 16px; }
.calendarPanel h2, .reportPanel h2 { margin: 0; }
.calendarPanel p, .reportPanel small { margin: 3px 0 0; color: #85899b; font-size: 12px; }
.calendarPanel input { border: 1px solid #d8dae5; border-radius: 8px; padding: 8px; }
.calendarWeek, .calendarGrid { display: grid; grid-template-columns: repeat(7, 1fr); }
.calendarWeek span { padding: 7px; color: #8b8fa1; text-align: center; font-size: 11px; }
.calendarDay, .calendarEmpty { min-height: 105px; padding: 7px; border-top: 1px solid #eceef3; border-right: 1px solid #eceef3; }
.calendarDay strong { display: block; margin-bottom: 5px; font-size: 12px; }
.calendarDay span { display: block; overflow: hidden; margin: 3px 0; padding: 3px 5px; border-radius: 4px; font-size: 10px; text-overflow: ellipsis; white-space: nowrap; }
.calendarDay small { color: #777b8f; font-size: 10px; }
.calendarPublished { color: #254fa3; background: #e0ebff; }
.calendarScheduled { color: #8b5700; background: #fff0c2; }
.calendarOffline { color: #5f6475; background: #e9eaf0; }
.operationsGrid { max-width: 1100px; margin: 0 auto; display: grid; grid-template-columns: 1fr 1fr; gap: 14px; }
.operationsGrid .reportPanel { width: 100%; min-width: 0; }
.reportPanel header a, .reportPanel header button { border: 1px solid #d8dae5; border-radius: 8px; padding: 7px 10px; color: #5548ba; background: white; font-size: 12px; text-decoration: none; cursor: pointer; }
.reportMetrics { display: grid; grid-template-columns: repeat(3, 1fr); gap: 8px; }
.reportMetrics span { padding: 10px; border-radius: 8px; color: #777b8f; background: #f4f5f8; font-size: 11px; }
.reportMetrics strong { display: block; color: #272a3d; font-size: 20px; }
.reportMetrics .reportBad strong { color: #b42318; }
.reportList { max-height: 220px; overflow: auto; margin-top: 12px; }
.reportList a { display: grid; grid-template-columns: 45px 1fr; gap: 6px; padding: 7px 0; border-bottom: 1px solid #eceef3; color: #5f6378; font-size: 11px; }
.reportList span { overflow: hidden; text-overflow: ellipsis; white-space: nowrap; }
.reportList p, .mediaSize { color: #85899b; font-size: 12px; }
.mediaSize { margin-top: 15px; }
.cleanupList { overflow: auto; padding: 20px; }
.cleanupNotice { padding: 10px; border-radius: 8px; color: #815600; background: #fff2ca; font-size: 12px; }
.cleanupList > div { display: flex; align-items: center; justify-content: space-between; gap: 12px; padding: 10px 4px; border-bottom: 1px solid #e5e7ed; }
.cleanupList > div > span { min-width: 0; display: grid; gap: 3px; }
.cleanupList strong { overflow-wrap: anywhere; font-size: 12px; }
.cleanupList small { color: #85899b; }
.cleanupList button { border: 1px solid #fecaca; border-radius: 7px; padding: 6px 9px; color: #b42318; background: white; cursor: pointer; }
.cleanupList details { margin-top: 18px; }
.cleanupList details p { overflow-wrap: anywhere; color: #6f7387; font-size: 11px; }
@media (max-width: 900px) {
  .studio { grid-template-columns: 1fr; }
  .sidebar { min-height: auto; max-height: 360px; }
  .grid { grid-template-columns: 1fr 1fr; }
  .toolbar { padding: 0 16px; }
  .toolbarActions { overflow-x: auto; }
  .form, .preview { padding: 24px 18px 60px; }
  .writingArea { grid-template-columns: 1fr; }
  .outline { display: none; }
  .modalBackdrop { padding: 10px; }
  .dashboard { padding: 24px 14px; }
  .metricGrid { grid-template-columns: repeat(2, 1fr); }
  .operationsGrid { grid-template-columns: 1fr; }
  .calendarDay, .calendarEmpty { min-height: 72px; }
}
